From f151985a89d3fab6ea16890f7f357f1b1b77844c Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?J=C3=B6rg=20Thalheim?= <joerg@thalheim.io>
Date: Thu, 6 Aug 2026 12:48:23 +0200
Subject: [PATCH] daemon: report unknown status when launchctl cannot inspect
 the GUI domain
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

launchctl print gui/<uid>/... fails with exit code 125 when the calling
session has no per-user launchd domain (sandboxed builds, plain SSH
sessions). Doctor should degrade to loaded=unknown instead of erroring.

Signed-off-by: Jörg Thalheim <joerg@thalheim.io>
---
 src/daemon/service.rs | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)

diff --git a/src/daemon/service.rs b/src/daemon/service.rs
index d35cf53..9ad301f 100644
--- a/src/daemon/service.rs
+++ b/src/daemon/service.rs
@@ -238,7 +238,10 @@ fn status_with_runner(runner: &impl LaunchctlRunner) -> Result<ServiceStatus> {
         .map(|program| paths_resolve_to_same_file(program, &current_executable));
     Ok(ServiceStatus {
         installed: spec.plist_path.exists(),
-        loaded: Some(service_is_loaded(runner, &spec)?),
+        // Report "unknown" instead of failing when launchctl cannot inspect
+        // the GUI domain (e.g. sandboxed builds or SSH sessions without a
+        // per-user launchd domain).
+        loaded: service_is_loaded(runner, &spec).ok(),
         plist_path: spec.plist_path,
         program,
         current_executable,
-- 
2.55.0

